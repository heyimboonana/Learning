#include <iostream>

int main(){
    std:: string operation;
    while(true){
        while(!(operation == "+" || operation == "-" || operation == "*" || operation == "/")){
            std::cout << "Enter an operation (+, -, *, / ): ";
            std::cin >> operation;
        }
        while()
    }
    return 0;
}